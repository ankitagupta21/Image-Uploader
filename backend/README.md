Backend contains two folders:

1. controllers:
   It contains a file named authController.js which contains functions for authentication on Login, Register and Forget password.

2. models:
   It contains a file named User.js which contains schema of user.

database.js is used to connect to mongoDB server.
index.js is used to connect to server.
